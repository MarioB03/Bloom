import SwiftUI

/// Habilidades: ejercicios de respiración, mindfulness y regulación emocional.
/// Equivalente a `app/(tabs)/habilidades.tsx` en la app React Native.
struct SkillsView: View {
    var body: some View {
        PlaceholderScreen(title: "Habilidades", systemImage: "sparkles")
    }
}

#Preview {
    SkillsView()
}
