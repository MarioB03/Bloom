import SwiftUI

/// Calendario emocional mensual.
/// Equivalente a `app/(tabs)/calendario.tsx` en la app React Native.
struct CalendarView: View {
    var body: some View {
        PlaceholderScreen(title: "Calendario", systemImage: "calendar")
    }
}

#Preview {
    CalendarView()
}
