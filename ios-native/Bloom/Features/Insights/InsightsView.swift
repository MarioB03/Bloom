import SwiftUI

/// Insights y correlaciones a partir de los check-ins.
/// Equivalente a `app/insights.tsx` en la app React Native.
struct InsightsView: View {
    var body: some View {
        PlaceholderScreen(title: "Insights", systemImage: "chart.line.uptrend.xyaxis")
    }
}

#Preview {
    InsightsView()
}
