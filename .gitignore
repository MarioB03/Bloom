# macOS
.DS_Store

# Claude Code
.claude/

# Local env files
.env
.env*.local
*.pem

# Firebase
.firebase/
firestore-debug.log

# Expo (residual de la app RN previa, por si reaparece)
.expo/

# Hosting (HTML legales servidos desde Firebase, no versionados)
hosting/

# Xcode (proyecto generado por XcodeGen — siempre se regenera)
ios-native/Bloom.xcodeproj
ios-native/Bloom.xcworkspace
ios-native/DerivedData/
ios-native/build/
ios-native/.swiftpm/
*.xcuserstate
xcuserdata/

# .storekit auto-generado por Xcode con el nombre del producto al archivar
ios-native/Bloom - Diario Emocional.storekit

# Credenciales / firma
*.p8
*.p12
*.jks
*.key
*.mobileprovision
